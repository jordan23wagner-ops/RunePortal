# Dependencies
node_modules/
*.pyc
__pycache__/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Local dev
*.log
temp/
.env
runeportal_*.backup
runeportal_*.local

# Personal / credential material — purged from history 2026-09-23.
# These still exist on the local machine, so without these rules a
# `git add -A` would put them straight back into a PUBLIC repo.
Jordon_Wagner_Resume.pdf
*_Resume.pdf
*_resume.pdf
# a mistyped shell command once saved itself as a filename, with a GitHub
# token inside it; the glob catches that whole class of accident
git remote set-url*
*.txt.tmp
